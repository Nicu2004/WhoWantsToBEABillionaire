services:
  postgres:
    image: postgres:14-alpine  # ← Use the official image
    container_name: postgres-container
    ports:
      - "5432:5432"
    volumes:
      - ~/apps/postgres:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=admin
      - POSTGRES_USER=postgres
      - POSTGRES_DB=test

